  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <ul class="nav" role="navigation">
          <li><%= link_to "Sound of", root_path, :class => 'brand' %><%= link_to " Fractions", root_path + 'songs#/index', :class => 'brand' %></li>
          <%= render 'layouts/shared/loadOrSaveSong' %>
        </ul>
        <ul class="nav pull-right login" role="navigation">
          <%= render 'layouts/shared/login' %>
        </ul>
      </div> <!-- End Container-fluid -->

    </div> <!-- End Navbar-inner -->
  </div> <!-- End Navbar -->

<div class="row-fluid">
  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
        <a class="close" data-dismiss="alert">&#215;</a>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      </div>
    <% end %>
  <% end %>
</div>